{
    "name": "stopallvirus-api",
    "description": "A RESTful web api for stop all virus",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.437.0"
    },
    "devDependencies": {
        "jest": "^24.7.1"
    },
    "scripts": {
        "test": "jest",
		"start": "sam local start-api -n env-dev.json"
    },
    "jest": {
        "verbose": false
    }
}
